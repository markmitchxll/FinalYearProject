/**
 * AppNavigator — defines the full navigation structure of the app.
 *
 * Structure:
 *  Bottom Tab Navigator
 *  ├── HomeScreen         — landing page / nightly summary
 *  ├── ARSkyScreen        — live AR camera view of the sky
 *  ├── PetScreen          — Tamagotchi pet
 *  ├── DiscoveriesScreen  — catalogue of everything the user has found
 *  └── MythologyScreen    — browse all myths and stories
 */

import React from 'react';

export default function AppNavigator() {
  // TODO: Create a Bottom Tab Navigator with the five screens above.
  // TODO: Style the tab bar with a dark/space theme.
  return null;
}
